import StoreList from "@/components/admin/stores/StoreList";

export const metadata = {
    title: 'Sucursales | Galaxia Admin',
    description: 'Gestión de puntos de venta y bodegas.',
};

export default function StoresPage() {
    return <StoreList />;
}